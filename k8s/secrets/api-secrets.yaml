apiVersion: v1
kind: Secret
metadata:
  name: api-secrets
  namespace: resume-builder
  labels:
    app: api
    app.kubernetes.io/name: api
    app.kubernetes.io/component: backend
    app.kubernetes.io/part-of: resume-builder
type: Opaque
stringData:
  # MongoDB Credentials
  # IMPORTANT: Replace these placeholder values with actual secrets in production
  # Consider using external secret management (e.g., AWS Secrets Manager, HashiCorp Vault)
  MONGODB_URI: "mongodb://admin:CHANGE_ME@mongodb:27017/resume_builder?authSource=admin"
  # Redis Credentials
  REDIS_HOST: "redis"
  REDIS_PASSWORD: "CHANGE_ME"
  # JWT Secrets
  JWT_SECRET: "CHANGE_ME_TO_A_SECURE_RANDOM_STRING"
  JWT_REFRESH_SECRET: "CHANGE_ME_TO_ANOTHER_SECURE_RANDOM_STRING"
  # S3/MinIO Credentials
  S3_ENDPOINT: "http://minio:9000"
  S3_ACCESS_KEY: "CHANGE_ME"
  S3_SECRET_KEY: "CHANGE_ME"
---
# Note: In production, use SealedSecrets, External Secrets Operator,
# or cloud provider secret management instead of plain Kubernetes secrets.
# Example with External Secrets Operator:
#
# apiVersion: external-secrets.io/v1beta1
# kind: ExternalSecret
# metadata:
#   name: api-secrets
#   namespace: resume-builder
# spec:
#   refreshInterval: 1h
#   secretStoreRef:
#     name: aws-secrets-manager
#     kind: ClusterSecretStore
#   target:
#     name: api-secrets
#   data:
#     - secretKey: MONGODB_URI
#       remoteRef:
#         key: resume-builder/mongodb
#         property: uri
